kind: ExternalService
name: pishe-backend
spec:
 image: registry.gitlab.com/voldemortapp/voldemort-backend:latest
 image_pull_policy: Always
 image_pull_secret: gitlab-cred
 port: 8080
 env:
 - name: DEBUG
   value: 1
